# Local development defaults. Copy this file to .env and adjust as needed.
APP_ENV=development
LOG_LEVEL=INFO

# Kafka is exposed to host clients only on the loopback interface.
KAFKA_HOST_PORT=29092
KAFKA_UI_PORT=8080
