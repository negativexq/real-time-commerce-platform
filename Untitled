Read AGENTS.md and inspect the entire repository before making changes.

Implement Sprint 0 only: repository foundation.

Create and configure:

- A clean Python 3.12 project foundation
- pyproject.toml
- Ruff linting and formatting configuration
- mypy configuration
- pytest configuration
- A minimal importable shared Python package
- A meaningful placeholder unit test
- .gitignore
- .env.example
- Makefile
- README.md skeleton
- A basic GitHub Actions CI workflow

The Makefile must provide:

- make lint
- make format
- make format-check
- make type-check
- make test
- make check
- make compose-config

Requirements:

- Keep the project compatible with Apple Silicon.
- Do not implement Kafka.
- Do not implement PostgreSQL.
- Do not implement Redis.
- Do not create application services yet.
- Do not create Docker Compose infrastructure yet.
- Do not add unnecessary Python dependencies.
- Do not place real secrets in any file.
- Preserve the existing repository structure where appropriate.

Before editing:

1. Summarize the constraints you found in AGENTS.md.
2. Show the proposed final file structure for Sprint 0.
3. List the files you will create or modify.

Then proceed with the implementation without asking for confirmation.

After implementation:

1. Run Ruff.
2. Run mypy.
3. Run pytest.
4. Report the exact commands and results.
5. Show the final changed-file summary.
6. Clearly state anything that could not be verified.